{
  "name": "@epub-reader/tauri-adapter",
  "version": "0.1.0",
  "description": "Tauri desktop adapters (SQLite storage and file operations) for the EPUB Reader",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsup src/index.ts --dts",
    "clean": "rimraf dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@epub-reader/core": "workspace:*",
    "@tauri-apps/api": "^1.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.11.25",
    "rimraf": "^5.0.5",
    "tsup": "^8.1.0",
    "typescript": "^5.3.3"
  }
}
